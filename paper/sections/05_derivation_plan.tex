\section{Derivation plan}

The first audit sequence should establish a source ledger.

\begin{enumerate}[label=(\arabic*)]
\item Import the Project 22 local-cell target.
\item Import the Project 23 reduced-universe theorem.
\item Import the canonical G60 local edge payload when available.
\item Inventory station provenance fields connected to the local target.
\item Test whether the four C paths arise from native G60 adjacency, quotient support, or transition support.
\item Test whether the anchor residue sets arise from native node-pair paths or lifted edge masks.
\item Test whether the ordinary/branch shell split is forced by native markers rather than labels.
\item Test whether the anchor twist \(B0,O0,O1,B1\) is forced by branch-anchor motion.
\end{enumerate}

A successful result would produce a theorem of the form:

\[
    \text{native provenance constraints}
    \Longrightarrow
    L=\{O0,O1,B0,B1\}.
\]
