\section{Inherited stack}

The current derivation stack is:

\[
\text{Project 22 local cell}
    \longrightarrow
\text{Project 23 reduced universe}
    \longrightarrow
\text{diagonal selector}.
\]

Project 22 established a local two-bit generator.  With shell bit \(b\) and rank bit \(r\), the four states are
\[
    O0=(0,0),\quad O1=(0,1),\quad B0=(1,0),\quad B1=(1,1).
\]

Project 23 then proved that the two readouts
\[
    C(L)=[O0,O1,B0,B1],
\]
\[
    A(L)=[B0,O0,O1,B1]
\]
generate the reduced 16-candidate universe
\[
    U=C(L)\times A(L),
\]
and that the hidden identity diagonal selects exactly
\[
    [1,6,8,15].
\]

Thus the next target is not the diagonal.  The next target is the origin of \(L\).
