\begin{abstract}
Project 22 generated a local Lift \& Twist answer-cell kernel from two state bits.  Project 23 then derived the reduced 16-candidate universe from that kernel as the product of two readouts, with hidden diagonal \([1,6,8,15]\) and visible permutation \([1,2,0,3]\).  The remaining upstream problem is now precise: derive the local cell itself from native G60 provenance.

This paper creates the proof surface for that upstream derivation.  Its target is not the full Gap A closure claim.  Its target is the smaller native-provenance bridge
\[
    \text{native G60 provenance}
    \longrightarrow
    \text{local Lift \& Twist answer cell}.
\]
At creation, this paper records a derivation target, not a completed theorem.
\end{abstract}
